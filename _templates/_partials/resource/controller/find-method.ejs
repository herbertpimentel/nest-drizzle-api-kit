@Get()
<% if (guardsDecorator) { %><%- guardsDecorator %>
<% } %><% if (docsDecorator) { %><%- docsDecorator %>
<% } %>async find(@Query() query: <%= queryType %>) {
  return this.service.find(query);
}
